<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tif.gaskeun.masodin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.CustomrMenuListActivity"
            android:label="@string/title_activity_customr__menu_list_"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.LoginActivity" />
        <activity android:name=".Controller.Dashboard" />
        <activity android:name=".Controller.RestSelector" />
        <activity android:name=".Controller.RestAdder" />
        <activity android:name=".Controller.MenuAdder" />
        <activity android:name=".Controller.OrderItem" />
        <activity android:name=".Controller.CheckOut" />
        <activity android:name=".Controller.GetDataAdmin" />
        <activity android:name=".Controller.ActivityAddAcount"/>
        <activity android:name=".Controller.AddMenu"/>
    </application>

</manifest>